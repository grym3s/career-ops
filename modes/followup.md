# Mode: followup — moved (LostAndLucky restructure, Phase 5)

This mode prompt moved to **`agents/followup-planner/prompt.md`**.

**Load `agents/followup-planner/prompt.md` instead.** See `agents/followup-planner/CONTEXT.md` for the agent contract. The runner script (`followup-cadence.mjs`) still lives at the repo root pending Phase 7.

This stub will be removed in Phase 7.
