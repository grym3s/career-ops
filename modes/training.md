# Mode: training — moved (LostAndLucky restructure, Phase 5)

This mode prompt moved to **`agents/training-eval/prompt.md`**.

**Load `agents/training-eval/prompt.md` instead.** See `agents/training-eval/CONTEXT.md` for the agent contract.

This stub will be removed in Phase 7.
