# Mode: patterns — moved (LostAndLucky restructure, Phase 5)

This mode prompt moved to **`agents/pattern-analyst/prompt.md`**.

**Load `agents/pattern-analyst/prompt.md` instead.** See `agents/pattern-analyst/CONTEXT.md` for the agent contract. The runner script (`analyze-patterns.mjs`) still lives at the repo root pending Phase 7.

This stub will be removed in Phase 7.
