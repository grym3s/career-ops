# Mode: latex — moved (LostAndLucky restructure, Phase 5)

This mode prompt moved to **`agents/pdf-generator/prompt-latex.md`** (same agent as the HTML/Playwright path — pdf-generator owns both rendering modes).

**Load `agents/pdf-generator/prompt-latex.md` instead.** See `agents/pdf-generator/CONTEXT.md` for the agent contract.

This stub will be removed in Phase 7.
