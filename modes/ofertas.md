# Mode: ofertas — moved (LostAndLucky restructure, Phase 4a)

This mode prompt moved to **`agents/evaluator/compare.md`** as part of the LostAndLucky folder restructure (see `wiki/decisions.md` 2026-05-19).

**Load `agents/evaluator/compare.md` instead.** Same agent as the single-offer evaluator (`agents/evaluator/prompt.md`) — comparing multiple offers is a different verb on the same domain, so it lives in the same agent folder.

This stub will be removed in Phase 7 (final cleanup).
